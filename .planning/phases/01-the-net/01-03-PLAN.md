---
phase: 01-the-net
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - test/run_tests.py
autonomous: true
requirements: [COVR-02, COVR-03, COVR-04]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All five Ashita event registrations, the text_in pcall boundary, settings load/save, the MUST_SAVE throttle, load-time resume, player-name resolution and the profile-switch handler are asserted against the shipped inctrack.lua (D-11)."
    - "Every /incursion subcommand — bare toggle, reset, lock, auto, usage — and visible()'s override-vs-auto logic are asserted (D-11)."
    - "FIX-01 and FIX-02 are each one named expected failure inside the state suite (D-09)."
    - "A guard asserts the known-defect count is exactly three, so a fourth failure or a fix that leaked in early is loud (D-06, D-14)."
    - "`python test/run_tests.py` with no chatlogs and no Ashita install reports a non-zero check count for both new suites, prints PASS and exits 0 (D-12, COVR-04)."
  artifacts:
    - "test/run_tests.py — test_addon_shell() suite function, wired into main()"
    - "test/run_tests.py — FIX-01 and FIX-02 xfails inside test_state_units"
    - "test/run_tests.py — EXPECTED_XFAILS constant and the main() guard"
  key_links:
    - "The shell suite reaches incursion, visible, reset, persist and MUST_SAVE through host.addon — inctrack.lua exports nothing and may not be edited."
    - "The load-time resume test round-trips through the stubbed json: one host persists, a second host restores from that exact string."
    - "The guard sums xfails across every suite in main(), so it holds in the no-chatlogs run and the full run alike."
---

<objective>
Bring `inctrack/inctrack.lua` — 301 lines at zero automated coverage — under
test behind the Ashita host stub, turn the two `state.lua` defects into named
expected failures, and close the phase with the guard that says *these three,
and nothing else, fail*.

Purpose: the profile-switch handler is the piece most likely to leak one
character's run into another's window, and the `MUST_SAVE` policy is the only
thing standing between a mid-run reload and a blank window for a whole phase.
Neither has ever been executed by a test. COVR-02 closes that; the guard is
what makes success criterion 4 checkable rather than asserted.

Output: one new suite in `test/run_tests.py`, two `xfail` entries added to the
existing state suite, and the `EXPECTED_XFAILS` guard in `main()`.

**The addon source is not modified by this plan.** `git diff -- inctrack/` must
be empty when the plan ends (D-15).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-the-net/01-CONTEXT.md
@.planning/phases/01-the-net/01-01-SUMMARY.md
@.planning/phases/01-the-net/01-02-SUMMARY.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md

@inctrack/inctrack.lua
@test/stubs.py
@test/run_tests.py
</context>

## Decision traceability

| ID | Locked decision (CONTEXT.md) | Where honoured |
|----|------------------------------|----------------|
| D-03 | The Ashita stub is a set of in-memory fakes; tests assert on resulting behaviour, not exact call sequences | Tasks 1, 2 |
| D-04 | A stubbed pure-Lua `json` so the suite needs no Ashita install | Task 1 (the resume round trip) |
| D-05 | `Result.xfail` alongside `check`; the report says `3 known defects (expected until Phase 2)`; the process still exits 0 | Task 3 |
| D-06 | A guard asserts the xfail count is exactly 3 | Task 3 |
| D-07 | The defect tests assert **intended** behaviour, never current behaviour | Task 3 |
| D-08 | Named in user terms | Task 3 |
| D-09 | FIX-01 and FIX-02 live in the state suite, flagged xfail, not quarantined | Task 3 |
| D-11 | `inctrack.lua` coverage: registration, `text_in` + pcall, settings load/save + `MUST_SAVE`, every `/incursion` subcommand, `visible()`, profile switch | Tasks 1, 2 |
| D-12 | The new suites never read chatlogs | Tasks 1, 2 |
| D-14 | The failure list is exactly three entries, each named so a reader who has never seen `state.lua` understands it | Task 3 |
| D-15 | The `git diff` emptiness check is part of verification | Plan verification |

## Hard rules for this plan

1. **Do not edit anything under `inctrack/`.**
2. **Exactly two new `xfail` entries.** Every other assertion in this plan must
   be true against the code as it ships. If a check goes red, it is either
   wrong or it is a fourth defect — stop and report it. Do not add it and do
   not convert it to an xfail; the phase closes on exactly three.
3. **Do not assert behaviour a later milestone is free to change.** In
   particular, `state.lua:360-363` accepts any player's points while the player
   name is unknown. That is a recorded risk with no v1 requirement, so this
   plan asserts nothing about the pre-name window in either direction — only
   that the name resolves, and that once it is known a foreign player's points
   are rejected.
4. **Do not assert that the manual-hide branch at inctrack.lua:219-222 works.**
   That is FIX-03, it already has its one expected failure in the ui suite, and
   a second entry would break the guard.
5. Suite title string must begin with `addon: ` so the report line is greppable.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The addon-shell suite — load, text_in, persistence, profiles</name>
  <files>test/run_tests.py</files>
  <behavior>
    - All five handlers register: load, unload, text_in, d3d_present, command, and the settings profile callback
    - The load banner names the addon version and reaches the captured chat exactly once
    - text_in with a **table** as `e.message` does not propagate: the pcall boundary catches it, one `parse error:` line reaches chat, and the run is untouched
    - text_in never sets e.blocked and never rewrites e.message
    - A chat line that is not Incursion-related leaves the run and the save counter alone
    - A MUST_SAVE event persists immediately; a phase line inside the five-second window does not; the same phase line past it does
    - The persisted session string is non-empty and decodes back to a table naming the instance
    - unload persists the in-progress run
    - A second host started with that exact session string resumes the run at load, and the chat says so
    - A corrupt session string is discarded whole at load: the setting is cleared, a save happens, and no run is left behind
    - With no party name available at load the player stays unset; once a name is available the next parsed event fills it in
    - Once the name is known a foreign player's points award is rejected and the player's own is counted
    - A profile switch resets the run, clears the player name, adopts the new settings table, and restores that profile's own saved session
    - A profile switch with no settings table saves and changes nothing else
  </behavior>
  <read_first>
    - inctrack/inctrack.lua lines 20-118 (requires, default_settings, the incursion table, MUST_SAVE, printf, now, persist, reset, visible)
    - inctrack/inctrack.lua lines 119-204 (the load handler, the unload handler, the text_in handler and its pcall boundary)
    - inctrack/inctrack.lua lines 275-301 (the settings profile-switch callback)
    - inctrack/state.lua lines 585-654 (restore — what the resume path must satisfy)
    - .planning/phases/01-the-net/01-01-SUMMARY.md (the host handle surface: fire, events, chat, settings, saves, sessions, switch_profile, set_party_name, tick, wall, addon)
  </read_first>
  <action>
Add a new suite function to `test/run_tests.py`, styled like the existing ones —
plain `test_<area>`, no chatlogs, its own hosts from `make_host()`, a `Result`
whose title begins with `addon: `. Place it after the ui suite and wire it into
`main()` last, so the existing report lines keep their order (D-11).

Reach the shell's file-scope state through `host.addon` — `incursion`,
`visible`, `reset`, `persist`, `MUST_SAVE` — since `inctrack.lua` exports
nothing and may not be edited. Assert on resulting behaviour rather than on
call sequences (D-03): what the run holds, what reached chat, how many saves
happened, what string was written.

Cover the behaviours listed above. Notes on the ones with a trap in them:

**The pcall boundary.** Force the error from outside rather than by editing the
addon: fire `text_in` with a **Lua table** as `e.message`. The handler's first
real act is a string-method call on it, which raises for a table, the `pcall`
catches, and `printf('parse error: %s', ...)` lands in chat.

Use a table (a boolean also works); do **not** use a number. Numbers share the
string metatable in Lua, so `(5):strip_colors()` coerces and succeeds — the
handler then runs to completion, `parser.parse` returns nil, and no
`parse error:` line is ever produced, so the test would silently assert nothing.
A table has no such metatable and is the fixture this check needs.

Assert one such line arrived,
that nothing propagated out to Python, and that the run is unchanged. Assert
too that the event table comes back with its message field intact and no
blocked field set — the read-only guarantee at inctrack.lua:156 is
comment-enforced today and this is the first test of it.

**The MUST_SAVE throttle.** Drive the injected clock, never wall time. Start at
zero, fire a begin line (a MUST_SAVE event) and expect one save; fire a phase
line immediately and expect no second save, because `save_at` was just stamped;
move the clock past the five-second window, fire the same phase line, and
expect the save. Then fire a boon line — also MUST_SAVE — and expect an
immediate save regardless of the window. Read the counter and the recorded
session strings from the host.

**The resume round trip.** Build a run in one host, fire `unload`, and take the
session string straight off that host's settings. Start a second host seeded
with exactly that string, fire `load`, and assert the run came back with its
instance and phase intact and that the resumed-run line reached chat. This
exercises the stubbed json in both directions (D-04) and is the closest the
suite gets to the real reload path. Then do the negative: a third host seeded
with a string that is not json at all must clear the setting, save, and leave
no run behind.

**Player-name resolution.** Start a host whose party-name accessor returns an
empty string, fire `load`, and assert the player is still unset. Make the name
available, fire a parsed event, and assert the deferred fetch at
inctrack.lua:176-181 filled it in. Then assert a foreign player's points award
is rejected and the player's own is counted. Assert nothing about the window
before the name resolves — see hard rule 3.

**The profile switch.** Build a run in the host, build a *different* character's
session string in a second host, then fire the profile callback with a new
settings table carrying that session. Assert the old run is gone, the new one
is restored, the player name is cleared so the new character's own points are
not filtered out, the shell adopted the new settings table, and a save
happened. Then fire the callback with no settings table and assert it saves and
changes nothing else.

Every fixture is an inline chat string using the established invented content
names (D-12). The suite opens no files and takes no chatlog argument.
  </action>
  <verify>
    <automated>python test/run_tests.py</automated>
    <automated>python test/run_tests.py | grep -E "^  addon: .*[1-9][0-9]* checks"</automated>
  </verify>
  <acceptance_criteria>
    - The report contains exactly one line beginning `  addon: ` with a check count greater than 25.
    - All six registration points — the five events plus the settings callback — are asserted present.
    - Firing `text_in` with a Lua table as `e.message` produces exactly one chat line containing `parse error` and raises nothing into Python. (A number would not do: it coerces through the string metatable and produces no such line.)
    - The save counter follows the MUST_SAVE policy exactly: 1 after the begin, still 1 after an immediate phase line, 2 after the same line past the five-second window, 3 after a boon line.
    - A run persisted by one host is resumed by a second host seeded with that session string, and the resumed-run line is in the second host's chat.
    - A session string that is not json leaves `settings.session` empty and no run behind.
    - After a profile switch the shell's run is the new profile's run and the player name is unset.
    - `python test/run_tests.py` prints `PASS`, exits 0, and its header still reads `chatlogs: none`.
    - `git diff --quiet -- inctrack/` exits 0.
  </acceptance_criteria>
  <done>The addon shell's load, chat, persistence and profile paths all execute outside the game, and a regression in any of them moves a check in the `addon: ` line.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The command surface, visible(), and the render gate</name>
  <files>test/run_tests.py</files>
  <behavior>
    - `/incursion` with no run sets the override on, blocks the command, and says there is nothing to show yet
    - `/incursion` a second time toggles the override back off
    - `/inc` is handled identically to `/incursion`
    - A command that is neither is not blocked and produces no chat
    - `/incursion reset` clears the run, empties the session, says so, and saves
    - `/incursion lock` flips the locked setting, saves, and reports the new state both ways
    - `/incursion auto` flips the auto setting, clears the override, saves, and reports the new state both ways
    - An unrecognised subcommand prints the four usage lines and nothing else
    - visible() is false with no run; true with a live run and auto on; false with auto off; true when the override is on even with auto off; false when the override is off with a live run
    - d3d_present draws nothing when visible() is false — zero ImGui calls recorded
    - d3d_present draws the window when visible() is true, and a normal frame leaves the override untouched
  </behavior>
  <read_first>
    - inctrack/inctrack.lua lines 96-115 (reset and visible)
    - inctrack/inctrack.lua lines 206-273 (the d3d_present handler and the whole command handler)
    - .planning/phases/01-the-net/01-02-SUMMARY.md (how the ui suite reads the recorder — reuse the same access pattern)
  </read_first>
  <action>
Extend the `addon: ` suite with the command surface and the visibility logic
(D-11).

Fire commands through the host's event helper, passing the command line as a
plain string — the harness's `string:args()` extension makes `args[1]` and
`args[2]` behave as they do in game. Read `blocked` back off the returned event
table.

For the toggles, read and set `override`, `auto` and `locked` through
`host.addon`'s `incursion` table and the live settings table rather than
inferring them from chat. Assert the chat text too, since the wording is the
only feedback the player gets: the lock and auto lines must report the state
they switched *to*, both ways.

For the usage path, fire a subcommand the handler does not know and assert the
four usage lines arrive and that nothing else happened — no reset, no setting
changed, no save.

For `visible()`, call the function directly out of `host.addon` and walk the
five combinations listed in `<behavior>`. This is the override-vs-auto
interaction that CONCERNS marks high priority, and it is three lines of code
with five distinct outcomes.

For the render gate, fire `d3d_present` and assert on the ImGui recorder:
nothing at all when `visible()` is false, and a window when it is true. Reset
the recorder between the two.

Do not arm the recorder's close switch anywhere in this suite, and do not
assert anything about the manual-hide branch — see hard rule 4. A normal frame
must leave `override` exactly as it found it; that is true today and stays true
under either Phase-2 fix, so it is safe to assert and worth asserting.
  </action>
  <verify>
    <automated>python test/run_tests.py</automated>
    <automated>python test/run_tests.py | grep -E "^  addon: .*checks +ok"</automated>
  </verify>
  <acceptance_criteria>
    - Both `/incursion` and `/inc` set `blocked` on the event; an unrelated command leaves it unset and produces no chat.
    - All four subcommands plus the bare toggle and the usage fallback are each asserted.
    - The lock and auto messages are asserted in both directions.
    - `visible()` is asserted across all five combinations of run present, auto, and override.
    - `d3d_present` with `visible()` false records zero ImGui calls; with it true, a `Begin` is recorded.
    - After a normal `d3d_present` frame, `override` holds the value it had before.
    - The `addon: ` line's check count is at least 20 higher than after Task 1 and its status column reads `ok`.
    - `python test/run_tests.py` prints `PASS` and exits 0.
    - `git diff --quiet -- inctrack/` exits 0.
  </acceptance_criteria>
  <done>Every branch of the `/incursion` command and every outcome of `visible()` is pinned, and the render gate is asserted from both sides.</done>
</task>

<task type="auto">
  <name>Task 3: FIX-01 and FIX-02 as named expected failures, and the exactly-three guard</name>
  <files>test/run_tests.py</files>
  <precondition>The chatlog directory `C:\Games\CatsEyeXI\catseyexi-client\Ashita\chatlogs` exists and holds the 126 `*.log` files — the phase's closing check is the full run against it, and without it the run is partial, not passing.</precondition>
  <read_first>
    - inctrack/state.lua lines 196-231 (the phase handler, one of the two writers of phases_cleared)
    - inctrack/state.lua lines 358-380 (the points handler, the other writer)
    - inctrack/state.lua lines 530-654 (serialise and restore — saved_at is written at :556 and used only for the staleness check at :597)
    - test/run_tests.py test_state_units in full (the suite these two entries join)
    - test/run_tests.py main() (where the guard is wired)
    - .planning/ROADMAP.md Phase 2 success criteria 2 and 3 (the intended behaviour these two assert)
  </read_first>
  <action>
Add exactly two `xfail` entries to `test_state_units` — the state suite, where
a defect of `state.lua` belongs (D-09) — and then the guard.

**FIX-01.** Build one run and measure `phases_cleared` as a *delta* at each
step, so the assertion holds under any single-author scheme Phase 2 chooses
rather than pinning one implementation:
  - a begin, then a **`Phase #1`** line, then read the count — a regular `check`
    that it is zero, which is true today. The phase number is load-bearing, not
    incidental: `state.lua:198` reads
    `if e.phase and e.phase - 1 > run.phases_cleared`, so `Phase #1` leaves the
    count at 0 while `Phase #2` would set it to 1. Use `Phase #1` here and
    number the later phase lines upward from it. Getting this wrong turns a
    correct implementation red, and hard rule 2 would then have you stop and
    report a fourth defect that does not exist;
  - a points award for the player followed by the next phase line (`Phase #2`),
    then read it again — a regular `check` that the boss kill moved it by
    exactly one, also true today;
  - a bonus objective announcement, its completion line, and a second points
    award for the player with no phase line between them, then read it a third
    time — the **xfail**: a payout that was not a boss kill must leave the count
    exactly where it was.
  - a regular `check` that both awards still added up in `points`, so a Phase-2
    fix that simply drops the second award is caught.

Today the third reading is one higher than the second, because
`state.lua:358-365` increments on every award. Name it in user terms (D-08),
in the register of *"counted a bonus payout as a cleared phase"*, and put the
two numbers in the message so the failure diagnoses itself.

**FIX-02.** Build a run with a 90-minute entry sync and a bonus that expires in
five minutes. Read `time_left` and `elapsed`, serialise, then simulate a
ten-minute unload by subtracting 600 from the blob's `saved_at` — the wall-clock
field the addon already writes and already reads for the staleness check, and
which is the correction term the restore path never applies. Restore into a
fresh state and assert as one entry:
  - the restored `time_left` is 600 seconds lower than the saved one;
  - the restored `elapsed` is 600 seconds higher than the saved one;
  - the bonus, which had only 300 seconds left when it was saved, is gone
    rather than shown.

Allow a two-second tolerance on the two time comparisons: `saved_at` is real
wall time and a second can tick between serialise and restore. Add a regular
`check` first that the restore was accepted at all — a run saved ten minutes
ago is well inside the three-hour window, and that part is true today.

Name it in user terms too, in the register of *"clock was optimistic by the
reload gap after a reconnect"*, with the observed and expected numbers in the
message. Set the shared injected clock explicitly at the start of this block and
return it to zero at the end, the way the timer suites already do — the runtime
is shared across suites.

**The guard (D-06, D-14).** Add a module-level constant for the expected count,
set to three, with a comment naming FIX-01, FIX-02 and FIX-03 and saying that
Phase 2 drives it to zero. In `main()`, after every suite has reported, sum the
expected failures across all suites and print a line reading
`3 known defects (expected until Phase 2)` — always in that form, with the
actual number substituted. If the total is not the expected count, print a
guard failure naming both numbers and fail the run: a fourth entry means
something regressed, a missing one means a fix leaked into this phase. Expected
failures alone must still leave the exit code at 0 (D-05).

The guard must hold identically in the no-chatlogs run and the full run, since
all three entries live in suites that always execute.

The closing verification counts lines containing `XFAIL` and requires exactly
three, and separately requires zero lines containing `FAILED`. Both counts rest
on the report markers plan 01-01 fixed — the `XFAIL ` prefix on every expected
failure, and the `NOW PASSING ` prefix on a no-longer-failing entry, which
carries neither substring. The guard line you add here must not contain either
one; `3 known defects (expected until Phase 2)` satisfies that as written.

Finally, update the module docstring's suite index to list the two new suites
and the three known defects, so the file explains itself to the next reader.
  </action>
  <verify>
    <automated>
python - <<'PY'
import subprocess, sys
out = subprocess.run([sys.executable, "test/run_tests.py"], capture_output=True, text=True)
assert out.returncode == 0, out.stdout
assert out.stdout.strip().endswith("PASS"), out.stdout
xf = [l.strip() for l in out.stdout.splitlines() if "XFAIL" in l]
assert len(xf) == 3, "expected exactly three known defects, got %d:\n%s" % (len(xf), "\n".join(xf))
assert "3 known defects (expected until Phase 2)" in out.stdout, out.stdout
assert len(set(xf)) == 3, "the three messages must be distinct: %s" % xf
for line in xf:
    for banned in ("state.lua", "ui.lua", "inctrack.lua", "phases_cleared",
                   "saved_at", "ARG_OPEN", "FIX-0"):
        assert banned not in line, "message is written in code terms, not user terms: %r" % line
print("OK")
PY
    </automated>
    <automated>test "$(python test/run_tests.py | grep -cE '^  (ui|addon): .*[1-9][0-9]* checks')" = "2"</automated>
    <human-check>Read the three known-defect lines in the report. Each must describe, in one sentence, something a player would notice and complain about — with no file name, no identifier and no line number. If a reader who has never opened state.lua cannot tell what went wrong, rewrite the message before closing the phase (D-08, D-14).</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "\.xfail(" test/run_tests.py` prints `3`.
    - `python test/run_tests.py` exits 0, its last line is `PASS`, and it prints `3 known defects (expected until Phase 2)`.
    - The report shows exactly three expected-failure lines, one in the `ui: ` suite and two in the state suite, with three distinct messages.
    - No expected-failure message contains a file name, a line number, or an identifier from the source.
    - Temporarily setting the expected-count constant to 2 makes the run print a guard failure and exit non-zero; restore it to 3 afterwards.
    - The FIX-01 entry is written as deltas in `phases_cleared`, not as one absolute expected number, so a Phase-2 fix that makes the phase line the sole author satisfies it.
    - FIX-01's first phase-line fixture is `Phase #1`, so the opening regular `check` that the count is zero holds against `state.lua:198` as shipped.
    - The FIX-02 entry asserts all three of time left, elapsed and the dropped bonus, with a two-second tolerance on the two time comparisons.
    - Full run: `python test/run_tests.py "C:\Games\CatsEyeXI\catseyexi-client\Ashita\chatlogs"` prints `PASS`, exits 0, shows the same three expected-failure lines and no line reading `FAILED`, and the summed check count is at least 12841 plus the two new suites' counts.
    - `git diff --quiet -- inctrack/` exits 0 and `git diff --cached --quiet -- inctrack/` exits 0.
  </acceptance_criteria>
  <done>The three confirmed defects are visible as three named, red, user-legible entries; a fourth or a missing one fails the run; and the suite still exits 0.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan owns:

| Artifact | Kind | Purpose |
|----------|------|---------|
| `test_addon_shell()` | suite fn | Suite 10. Title begins `addon: `. Registration, `text_in` + pcall, persistence and `MUST_SAVE`, resume, player name, profile switch, commands, `visible()`, render gate |
| FIX-01 xfail | assertion | "counted a bonus payout as a cleared phase", inside `test_state_units` |
| FIX-02 xfail | assertion | "clock was optimistic by the reload gap after a reconnect", inside `test_state_units` |
| `EXPECTED_XFAILS` | constant | Three, with a comment naming the defects and Phase 2's job |
| `main()` guard | check | Sums expected failures across suites; fails the run on any number but three (D-06) |
| `main()` wiring for suite 10 | edit | Appended after the ui suite |
| Updated module docstring | edit | Suite index now lists ten suites and the three known defects |

Phase-wide roll-up of every artifact: see `01-01-PLAN.md`, section
"Artifacts this phase produces".

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chat stream → points and boon attribution | `state.lua:358-384` filters to the player, but only once a name is known |
| persisted session string → `restore()` | Well-formed json of the wrong shape is adopted without type checks (`state.lua:606`) |
| author's private chatlogs → test output | Suites 1-3 read real logs containing a real character name |
| harness → repository working tree | `inctrack/` must stay byte-identical |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Spoofing | points and boon attribution in `state.lua` | medium | mitigate | The shell suite asserts that once the player name has resolved, a foreign player's award is rejected and the player's own is counted. The unknown-name acceptance window is a recorded risk with no v1 requirement and is deliberately left unasserted rather than silently blessed — see hard rule 3 |
| T-01-06 | Information disclosure | private chatlogs | medium | mitigate | Both new suites read no files and take no chatlog argument (D-12); every fixture is an inline string using invented content names, so a contributor's run cannot embed the author's logs or character in the new coverage |
| T-01-08 | Tampering | structurally invalid restored session | medium | accept | The shell suite proves a non-json session is discarded whole. Well-formed json of the wrong shape reaching arithmetic is HARD-05 and belongs to Phase 3; asserting a fix here would create a fourth failure and break the guard |
| T-01-07 | Tampering | `inctrack/` addon source | high | mitigate | `git diff --quiet -- inctrack/` and `git diff --cached --quiet -- inctrack/` are acceptance criteria on this plan's closing task, and the phase does not close until both exit 0 |
| T-01-SC | Tampering | package-manager installs | low | accept | No package-manager install task exists in this plan; `lupa` is pre-existing |
</threat_model>

<verification>
This is the phase's closing verification. All five must hold.

1. `python test/run_tests.py` — prints `PASS`, exits 0, header reads
   `chatlogs: none (...)`, and both the `ui: ` and `addon: ` lines show non-zero
   check counts (COVR-04, ROADMAP criterion 3).
2. `python test/run_tests.py "C:\Games\CatsEyeXI\catseyexi-client\Ashita\chatlogs" | tee /tmp/inctrack-phase1-run.txt | tail -1`
   — prints `PASS`, exit code 0.
3. `grep -oE '[0-9]+ checks' /tmp/inctrack-phase1-run.txt | awk '{s+=$1} END {print s}'`
   — at least 12841, and `grep -c FAILED /tmp/inctrack-phase1-run.txt` is `0`.
   Suite 2 still reports that every real line was handled by a specific pattern.
4. `grep -c XFAIL /tmp/inctrack-phase1-run.txt` is exactly `3`, and
   `grep -c "3 known defects (expected until Phase 2)" /tmp/inctrack-phase1-run.txt`
   is `1` (ROADMAP criterion 4).
5. `git diff --quiet -- inctrack/ && git diff --cached --quiet -- inctrack/`
   — both exit 0 (ROADMAP criterion 5, D-15).
</verification>

<success_criteria>
- Suite `addon: ` covers every item CONTEXT lists for `inctrack.lua`: registration, `text_in` and its pcall boundary, settings load/save and the `MUST_SAVE` throttle, resume, player-name resolution, every `/incursion` subcommand, `visible()`, the render gate, and the profile-switch handler.
- FIX-01 and FIX-02 are two named expected failures in the state suite, written as intended behaviour and phrased in user terms.
- The failure list from a full run is exactly three entries, one per confirmed defect, and the guard says so.
- No previously-green suite regresses and the summed check count does not fall below 12841.
- Both new suites report non-zero check counts with no chatlogs and no Ashita install.
- The process exits 0 in both run modes.
- `git diff -- inctrack/` is empty.
</success_criteria>

<output>
Create `.planning/phases/01-the-net/01-03-SUMMARY.md` when done.
Record in it: the final check count in both run modes, the exact wording of all
three known-defect messages, the `addon:` and `ui:` suite counts, and — for
Phase 2 — the precise location of each xfail and the note that its assertion
text is not to be edited when the fix lands.
</output>
